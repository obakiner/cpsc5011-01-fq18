#ifndef GREET_H_
#define GREET_H_
#include <string>
using namespace std;

/*
 * This is a standard declaration file -- it contains the
 * signature of the function without its implementation.
 * The #ifndef stuff is to make sure the file only gets included once.
 *
 */

string greet(string s);

#endif /* GREET_H_ */
